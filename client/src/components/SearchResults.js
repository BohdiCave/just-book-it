import React from 'react';

export default SearchResults() {
    return(
        <section id="search-results">
             {/* Conditionality based on API results - load array of results in a loop  */}
            <Card/>
        </section>
    );
}